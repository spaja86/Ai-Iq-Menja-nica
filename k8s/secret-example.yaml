# This is an EXAMPLE file. DO NOT commit actual secrets to version control.
# Create actual secrets using kubectl create secret or sealed-secrets

apiVersion: v1
kind: Secret
metadata:
  name: crypto-exchange-secrets
type: Opaque
stringData:
  # Database
  database-url: "postgresql://user:password@postgres:5432/crypto_exchange"
  
  # Security
  secret-key: "CHANGE_THIS_TO_A_RANDOM_SECRET_KEY"
  
  # Payment Providers
  stripe-api-key: "sk_live_..."
  stripe-webhook-secret: "whsec_..."
  paypal-client-id: "..."
  paypal-client-secret: "..."
  
  # KYC Provider
  kyc-api-key: "..."
  kyc-secret-key: "..."
  
  # Email
  smtp-user: "..."
  smtp-password: "..."
  
  # Monitoring
  sentry-dsn: "..."
